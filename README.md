# ml-eof-framework-for-enso-prediction
A hybrid machine-learning and EOF-based framework for long-lead ENSO prediction using ocean–atmosphere predictors (Niño-3.4 SST, SOI, and warm water volume). The model integrates EOF-based dimensionality reduction with ridge regression and random forest to improve forecast skill up to 24 months.
